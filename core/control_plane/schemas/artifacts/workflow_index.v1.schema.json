{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:watchtower:schema:artifacts:indexes:workflow-index:v1",
  "title": "Workflow Index",
  "description": "Machine-readable lookup index for governed workflow modules and any task-specific extra files they require beyond the routing baseline.",
  "type": "object",
  "required": [
    "$schema",
    "id",
    "title",
    "status",
    "entries"
  ],
  "properties": {
    "$schema": {
      "const": "urn:watchtower:schema:artifacts:indexes:workflow-index:v1"
    },
    "id": {
      "type": "string",
      "const": "index.workflows"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "status": {
      "$ref": "#/$defs/lifecycleStatus"
    },
    "entries": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/workflowEntry"
      }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "lifecycleStatus": {
      "type": "string",
      "enum": [
        "draft",
        "active",
        "deprecated"
      ]
    },
    "idPattern": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(\\.[a-z0-9]+(?:_[a-z0-9]+)*)+$"
    },
    "docPath": {
      "type": "string",
      "pattern": "^workflows/modules/.+\\.md$"
    },
    "referenceDocPathList": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^docs/references/.+\\.md$"
      }
    },
    "stringList": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "urlList": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^https?://.+$"
      }
    },
    "workflowEntry": {
      "type": "object",
      "required": [
        "workflow_id",
        "title",
        "summary",
        "status",
        "doc_path",
        "uses_internal_references",
        "uses_external_references"
      ],
      "properties": {
        "workflow_id": {
          "$ref": "#/$defs/idPattern"
        },
        "title": {
          "type": "string",
          "minLength": 1
        },
        "summary": {
          "type": "string",
          "minLength": 1
        },
        "status": {
          "$ref": "#/$defs/lifecycleStatus"
        },
        "doc_path": {
          "$ref": "#/$defs/docPath"
        },
        "uses_internal_references": {
          "type": "boolean"
        },
        "uses_external_references": {
          "type": "boolean"
        },
        "related_paths": {
          "$ref": "#/$defs/stringList"
        },
        "reference_doc_paths": {
          "$ref": "#/$defs/referenceDocPathList"
        },
        "internal_reference_paths": {
          "$ref": "#/$defs/stringList"
        },
        "external_reference_urls": {
          "$ref": "#/$defs/urlList"
        },
        "aliases": {
          "$ref": "#/$defs/stringList"
        },
        "tags": {
          "$ref": "#/$defs/stringList"
        },
        "notes": {
          "type": "string",
          "minLength": 1
        }
      },
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "properties": {
              "uses_external_references": {
                "const": true
              }
            }
          },
          "then": {
            "required": [
              "external_reference_urls"
            ]
          }
        }
      ]
    }
  }
}
