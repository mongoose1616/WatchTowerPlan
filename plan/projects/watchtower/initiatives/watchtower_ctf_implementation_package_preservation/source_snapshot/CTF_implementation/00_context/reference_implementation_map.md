# Reference Implementation Map

## `/home/j/WatchTowerPlan/core`

Role:

- canonical shared contract, shared governance surfaces, reusable loaders, validation, query, sync, rebuild, and pack integration/export flows.

Use for:

- pack interface contract;
- governed surface loading;
- schema and validator patterns;
- bootstrap/export/validation commands;
- shared workflow metadata and route-preview model.

## `/home/j/WatchTowerPlan/plan`

Role:

- working hosted-pack implementation with broad pack-owned registries, indexes, workflows, and command surfaces.

Use for:

- full pack settings surface declarations;
- rendered/indexed pack machine roots;
- phase/guide/decision planning artifact patterns;
- command namespace and integration descriptor conventions.

## `/home/j/WatchTowerOversight/oversight`

Role:

- working hosted-pack implementation with focused pack-owned workflow metadata and repo-specific document-semantics validation.

Use for:

- compact but real workflow metadata registry patterns;
- pack-owned validation package shape;
- routed workflow and role composition;
- evidence/review-oriented doc and guide patterns.

## `/home/j/WatchTower`

Role:

- empty target destination for the copied shared core and the future offensive-security pack.

Use for:

- destination assumptions only;
- no source authority should be derived from this repo until the exported core and hosted pack are added.
