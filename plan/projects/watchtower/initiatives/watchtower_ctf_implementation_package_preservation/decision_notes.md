# WatchTower CTF Implementation Package Preservation Decision Notes

## Summary

The imported package no longer carries open phase-gated implementation decisions. All `86` rows in the preserved decision register are `locked_in_package`, and this initiative preserves that outcome as the canonical basis for follow-on WatchTower implementation through the exact row-by-row appendix below. The full raw register remains mirrored at `source_snapshot/CTF_implementation/indexes/open_decisions.json.raw`, while trigger-based later revisits are captured separately in `conditional_revisit_queue.md` and already-reconciled tensions are summarized in `contradiction_sweep_ledger.md`.

## Preservation Decisions Locked By This Initiative

- Preserve the full donor package rather than summarizing it externally.
- Use a transformed mirror so donor JSON companions remain byte-identical but validator-safe inside the initiative.
- Keep the mirror immutable after capture; changes to imported meaning happen in canonical initiative docs or in a future explicit recapture.
- Keep support manifests beside the canonical docs rather than forcing them into initiative-state `authored_inputs`, because the initiative schema only accepts the four canonical doc kinds.
- Treat the initiative as the new durable planning authority for this package, while still keeping current repo machine-readable surfaces above imported workbook history when conflicts exist.
- Keep this same initiative as the engineer handoff anchor rather than creating a successor initiative for execution planning, and express readiness through a phase-aligned live task chain instead of a second planning package.
- Keep a separate contradiction-sweep ledger so resolved tensions remain visible without inflating the locked decision appendix.

## Locked Current-Compatible Baseline

- `pack_root = offensive_security`
- `workspace_root = offensive_security/`
- `pack_slug = offensivesecurity`
- `pack_id = pack.offensivesecurity`
- `command_namespace = offsec`
- `domain_roots = ctf, knowledge`
- canonical challenge path remains `offensive_security/ctf/<platform>/<event>/<challenge>/` with stable placeholder segments in v1
- shared-core surfaces `governance_surface_map`, `path_pattern_registry`, `status_registry`, and `actor_registry` are adopted rather than treated as future work
- challenge and session lifecycle artifacts adapt to the shared status vocabulary, carrying solve and unresolved semantics in closeout outcome rather than pack-only status values
- the first implementation target remains the empty `/home/j/WatchTower` repo, reached through export, copy, scaffold, bootstrap, and validate rather than manual donor copying

## Exact Live Contract Delta Register

| Delta ID | Earlier Assumption | Live Contract / Proof | Disposition |
|---|---|---|---|
| `delta.slug.current_compatible` | `STEP1_FINAL_v3.md` recommends underscore-safe identifiers such as `pack_slug = offensive_security` | live scaffold proof passes with `pack_slug = offensivesecurity`, `pack_id = pack.offensivesecurity`, `command_namespace = offsec` | keep scaffold-spec runnable identity as baseline |
| `delta.actor_registry.present` | v3 treats `actor_registry` as review / extension territory | shared core now ships `actor_registry.json` and typed schema plus loader support | adopt in Phase 2 pack machine contract |
| `delta.governance_surface_map.present` | older workbook phases treat governed surface map as future extension | shared core now ships `governance_surface_map.json` and typed schema plus resolver support | adopt in Phase 2 pack machine contract |
| `delta.path_status_registries.present` | older workbook treats several governance surfaces as pack-only future work | shared core now ships `path_pattern_registry.json` and `status_registry.json` | reuse shared surfaces and extend pack-local registries only where necessary |
| `delta.status_subset.current_compatible` | Step 1 often used challenge-local `solved` and `unresolved` semantics directly in artifact lifecycle sketches | live shared `status_registry.json` uses current-compatible global values such as `active`, `blocked`, `completed`, `closed`, `in_review`, and `needs_review`, but not `solved` or `unresolved` | adapt challenge/session `status` subsets to shared-core values and carry solve/unresolved semantics in `closeout_record.outcome` instead |
| `delta.event_stream_contract.reconciled` | the preserved decision register row for `decision.event_stream_contract` uses shorthand that implies required `actor` and `session_id` | the package’s field-level state/index contract and implementation-gap audit both lock `event_id`, `event_type`, `timestamp_utc`, and `challenge_id` as required, with `session_id` and `actor_ref` conditional | follow the state/index contract for implementation; preserve the raw decision row as historical source evidence without treating it as the authoritative field-cardinality contract |
| `delta.workflow_catalog.not_required` | older workbook repeatedly assumes a future stronger `workflow_catalog` model | current contract still centers routing table + workflow metadata registry + workflow/route indexes | lock `workflow_catalog` as a post-v1 deferral, not a baseline requirement |
| `delta.same_initiative_handoff` | a successor initiative could have been created after preservation to host execution tasking | the current repo can keep one approved initiative package with a phase-aligned live task chain, one initiative-local validation bundle, and rebuilt trace joins without splitting planning authority | keep the preserved initiative as the active engineer handoff package and use ready tasking plus trace joins instead of a successor initiative |
| `delta.domain_packs.retired` | early docs use `domain_packs/offensive_security/...` | current shared docs and scaffold use first-party root pack `offensive_security/` | reject retired topology |
| `delta.challenge_path.v1_placeholder_segments` | later Step 1 refinements collapse missing `platform` or `event` path segments entirely | the v1 package keeps the scaffold-compatible fixed root `offensive_security/ctf/<platform>/<event>/<challenge>/` and uses stable placeholders such as `unknown_platform` and `unknown_event` instead | keep placeholder segments in v1 and defer optional-segment collapse beyond v1 |
| `delta.target_repo.empty` | older docs assume a fuller target repo context | `/home/j/WatchTower` is empty except `.git` | plan export/bootstrap as first real content landing |

## Exact Normalized Decision Register

This appendix restates every row from `source_snapshot/CTF_implementation/indexes/open_decisions.json.raw` with its exact locked default, affected phases, and upstream dependency.

It contains the full normalized set of `86` preserved decision rows. Later summary sections may intentionally restate a subset of those same decision ids for operator scanability, but those restatements do not add new preserved decisions beyond this appendix.

- `decision.query_human_output_contract` (area: `query_runtime`; phases: `phase.3, phase.5`; upstream: `none_pack_owned`): curated queries support --format human|json|both with JSON as the default posture; human output is table-first plus one-line summaries where helpful, uses shared compact/standard/verbose presets, command-specific default row limits capped at roughly twenty, command-specific visible columns capped at six before expansion, explicit truncation and zero-result guidance, compact material trust/provenance cues, and treats status as the main operator-context command alongside a shared human structure for status, challenges, and sessions
- `decision.curated_relation_expansion_contract` (area: `query_runtime`; phases: `phase.3, phase.5`; upstream: `none_pack_owned`): curated family queries may expand relations only through one shared --expand-relations surface with one-hop default unless depth is explicit, separate expansion limits, a default per-root cap of five targets, shared relation/family/trust/review filter flags aligned with graph query, graph suggestions only on truncation, focus support limited to knowledge/artifacts/events/discrepancies, and one shared --sort contract with clear failures on unsupported fields
- `decision.graph_human_output_contract` (area: `graph_query`; phases: `phase.3, phase.5`; upstream: `none_pack_owned`): public graph query is core for agents but advanced for humans; human graph output defaults to depth-two capped traversal, root-summary plus relation-group presentation, repeated-node dedupe, inline provenance markers, root-plus-immediate-neighbor summaries, and named operator/provenance/retrieval/review modes as alias bundles over shared flags
- `decision.knowledge_human_output_contract` (area: `knowledge_retrieval`; phases: `phase.3, phase.5`; upstream: `none_pack_owned`): knowledge human output always labels result families, groups only when multiple families appear, uses the fixed human family order tactics/playbooks/tools/commands/references then everything else, keeps JSON ranking pure, distinguishes tactics strongly, exposes parent/support/tool cues for playbooks/commands/references, leaves filtered commands and references flatter by default with conditional subgrouping only when mixed-tool or mixed-target result sets appear, and reuses the same material-only why-this-matched explanation pattern across knowledge, commands, and references while clearly signaling included or excluded candidate material
- `decision.runtime_rollout_sequence` (area: `implementation_rollout`; phases: `phase.1, phase.2, phase.3`; upstream: `target_repo_integration`): start runtime implementation with workflow metadata/docs plus the first machine-record bundle together, prove a thin real-CLI vertical slice on the actual pack root through challenge_intake -> challenge_metadata + notes_metadata + event_stream + artifact_index + query challenges + query artifacts using real schemas and validators, add unit tests plus CLI smoke tests immediately, then prove status, then blockers, then knowledge, ship graph after knowledge, run a short packaging/UX consolidation pass, and use a deliberately small/simple target for the first real challenge run; after consolidation choose the next capability by highest operator pain point with agent leverage as the secondary tie-break
- `decision.routing_contract` (area: `workflow_routing`; phases: `phase.1, phase.3`; upstream: `none_pack_owned`): treat ROUTING_TABLE.md and workflow_metadata_registry.json as co-equal authoritative routing surfaces; validate both together and fail immediately if they disagree
- `decision.slug_id_generation` (area: `identity_and_paths`; phases: `phase.1, phase.2, phase.4`; upstream: `none_pack_owned`): normalize platform, event, challenge, and knowledge slugs to lowercase snake_case ASCII; derive challenge_id as challenge.<platform_slug>.<event_slug>.<challenge_slug>; derive knowledge_id as knowledge.<family>.<knowledge_slug> or knowledge.<family>.<context_slug>.<knowledge_slug> when the family naturally nests under a stable parent; keep path and id suffixes aligned when collisions require numeric disambiguation
- `decision.challenge_path_fallback_policy` (area: `identity_and_paths`; phases: `phase.1, phase.2, phase.4`; upstream: `none_pack_owned`): keep the current-compatible canonical challenge root as offensive_security/ctf/<platform>/<event>/<challenge>/ in v1; use stable placeholder segments such as unknown_platform and unknown_event when source data is missing; when better metadata becomes available, run a governed rename that rewrites known refs, indexes, rendered links, and machine records atomically; defer optional-segment collapse beyond v1
- `decision.challenge_lifecycle_statuses` (area: `lifecycle_model`; phases: `phase.2, phase.4, phase.5`; upstream: `none_pack_owned`): use the current-compatible shared-status subset active, blocked, in_review, needs_review, completed, and closed for challenge artifacts; carry solved or unresolved semantics in closeout_record.outcome rather than as pack-only status values
- `decision.session_lifecycle_statuses` (area: `lifecycle_model`; phases: `phase.2, phase.4, phase.6`; upstream: `none_pack_owned`): use the current-compatible shared-status subset active, blocked, in_review, needs_review, completed, closed, and cancelled for session artifacts; represent pause and handoff through explicit session-state fields such as pause_reason and handoff_ready; allow only one active session per challenge
- `decision.lifecycle_transition_policy` (area: `lifecycle_policy`; phases: `phase.2, phase.4, phase.6`; upstream: `none_pack_owned`): publish pack-local status_transition_rules for challenge_state, session_state, knowledge_state, closeout_record, and extraction_output instead of encoding lifecycle behavior only in code
- `decision.status_transition_rules_contract` (area: `lifecycle_policy`; phases: `phase.2, phase.4, phase.6`; upstream: `none_pack_owned`): use one pack-local status_transition_rules policy artifact with entries keyed by lifecycle family and transitions keyed by from_state, to_state, and review_required, following the donor-pack oversight pattern; lock starter entries for challenge_state, session_state, knowledge_state, closeout_record, and extraction_output with explicit reopen and review-gated edges where required
- `decision.event_stream_contract` (area: `event_stream`; phases: `phase.2, phase.4, phase.6`; upstream: `none_pack_owned`): use one stable event_stream envelope with required event_id, event_type, timestamp_utc, actor, session_id, and challenge_id fields; keep event-type-specific detail in payload and add only the locked conditional top-level refs such as mode, route, artifact refs, status transitions, and reason when relevant
- Reconciliation note for `decision.event_stream_contract`: the raw decision row above is preserved verbatim from `open_decisions.json.raw`, but the implementation-facing field contract is governed by `04_contracts/state_and_index_contracts_plan.md` and the package’s implementation-gap audit. In canonical v1 use `actor_ref` as the actor-linked event field when present, do not add a separate `actor` envelope field, and treat `session_id` as conditional rather than universally required.
- `decision.event_type_registry_contract` (area: `event_stream`; phases: `phase.2, phase.3, phase.4, phase.7`; upstream: `none_pack_owned`): publish a pack-local event_type_registry with the locked starter taxonomy from the state/index contract, including lifecycle, command, workflow, review, blocker, transfer, session, discrepancy, and sync or rebuild event classes
- `decision.artifact_index_contract` (area: `artifact_index`; phases: `phase.2, phase.4, phase.5`; upstream: `none_pack_owned`): require artifact_id, artifact_family, path, pack, status, authoritative, hidden, derived, created_at_utc, and updated_at_utc on every artifact_index entry, then allow the locked conditional challenge, source, review, trust, discrepancy, and rendered-view fields as family-specific extensions
- `decision.environment_context_contract` (area: `environment_context`; phases: `phase.2, phase.4, phase.6`; upstream: `none_pack_owned`): use one governed root record with nested environment_context; require environment_context.summary and environment_context.type, keep the locked starter type, constraint, and capability vocabularies, and flatten the standard environment_* fields into pack indexes
- `decision.challenge_metadata_contract` (area: `challenge_metadata`; phases: `phase.2, phase.4`; upstream: `none_pack_owned`): store ctf_challenge_metadata at .wt_local/challenge_metadata.json with required challenge_id, challenge_slug, canonical_path, challenge_path, notes_path, nested source, status, created_at_utc, and updated_at_utc fields; allow conditional platform/event slugs, display title, review cues, current session, blocker/discrepancy counts, last activity, rendered_view_path, and latest_closeout_ref; do not duplicate challenge.md body prose into JSON
- `decision.notes_metadata_contract` (area: `notes_metadata`; phases: `phase.2, phase.4`; upstream: `none_pack_owned`): store ctf_notes_metadata at .wt_local/notes_metadata.json with required challenge_id, notes_path, reconciliation_state, updated_at_utc, unresolved_blocker_count, and unresolved_discrepancy_count fields; allow conditional checksum, edit timestamps, reconciliation timestamp, current session, last editor actor, and summary-presence cues; keep notes.md authoritative for working narrative
- `decision.session_state_contract` (area: `session_state`; phases: `phase.2, phase.4, phase.6`; upstream: `none_pack_owned`): store ctf_session_state at .wt_local/session_state.json with required session_id, challenge_id, status, requested_mode, effective_mode, interaction_mode, environment_context_ref, started_at_utc, and last_activity_at_utc fields; allow conditional workflow/route refs, summaries, recent command/evidence refs, blocker ref, pause_reason, handoff_ready, handoff_notes, resume linkage, close fields, and review notes
- `decision.source_trust_model` (area: `provenance`; phases: `phase.2, phase.5, phase.6`; upstream: `none_pack_owned`): use one nested authoritative source object with required source.summary and optional source.platform, source.event, source.url, source.ref, and source.type; publish source_type values derived_challenge_knowledge, curated_reference, local_taxonomy, challenge_page, challenge_prompt, event_material, official_standard, official_docs, vendor_docs, high_quality_reference, community_reference, internal_observation, local_file, and paste; publish trust_state values unknown, unverified, source_attested, partially_verified, and verified; publish verification_status values not_reviewed, inspected, integrity_checked, manually_confirmed, and reproduced; allow per-family subsets
- `decision.validation_suite_layout` (area: `validation`; phases: `phase.2, phase.3, phase.7`; upstream: `shared_validation_hooks_available`): keep one suite.offensivesecurity.validation_baseline with pack_contract, front_matter, document_semantics, artifact, graph_index, authority_map, query_contracts, and lifecycle_policy steps, then add later suites only if offsec-specific gates truly need separate execution
- `decision.pack_control_registries` (area: `control_plane_surfaces`; phases: `phase.2, phase.3, phase.4, phase.5`; upstream: `none_pack_owned`): include artifact_family_registry, documentation_family_registry, template_catalog, human_surface_policy_registry, authority_map, rendered_surface_registry, query_family_registry, event_type_registry, discrepancy_type_registry, severity_registry, discrepancy_resolution_registry, and governance_limit_registry in v1, then add source_type_registry, trust_state_registry, verification_status_registry, relation_type_registry, review_status_registry, and promotion_policy_registry as the relevant phases land
- `decision.controlled_registry_shape` (area: `control_plane_surfaces`; phases: `phase.2, phase.3, phase.5`; upstream: `shared_registry_patterns_available`): keep one authoritative JSON artifact per registry with current-compatible root fields such as $schema, id, title, and status; use entries[] for entry registries, surfaces[] for rendered registries, and values[] for controlled-value registries; for controlled values use value, entry_status, description, and optional aliases, replacement, notes, and examples; generate human-readable registry companions from authoritative JSON rather than hand-authoring them
- `decision.template_catalog_contract` (area: `control_plane_surfaces`; phases: `phase.2, phase.4`; upstream: `none_pack_owned`): lock template_catalog entries to template_id, entry_status, authorship_mode, template_path, required_section_ids, optional_section_ids, section_order, and allowed_roots, with conditional surface_id, family_id, front_matter_schema_id, and section_spec_schema_id fields plus structured llm_guidance
- `decision.documentation_family_registry_contract` (area: `control_plane_surfaces`; phases: `phase.2, phase.4, phase.5`; upstream: `none_pack_owned`): lock documentation_family_registry entries to family_id, entry_status, summary, allowed_roots, authorship_mode, and required_index_ids, with conditional front_matter schema, template, section spec, and mirror fields
- `decision.human_surface_policy_registry_contract` (area: `control_plane_surfaces`; phases: `phase.2, phase.3`; upstream: `none_pack_owned`): lock human_surface_policy_registry entries to policy_id, path_pattern, match_mode, root_kind, entry_status, governing_surfaces, clarifying_rule, and surfaces[], with each surface declaring relative_path, entity_shape, surface_role, mode, and authorship_mode
- `decision.rendered_surface_registry_contract` (area: `rendered_surfaces`; phases: `phase.2, phase.3`; upstream: `none_pack_owned`): lock rendered_surface_registry to surface_id, title, output_path, and sections[] with allowed section kinds table, bullet_summary, lines, and updated_at, plus stable column formatter contracts for table sections
- `decision.authority_question_set` (area: `authority_routing`; phases: `phase.2, phase.3, phase.6`; upstream: `none_pack_owned`): publish v1 authority questions for current pack state, challenge lookup, blocker/discrepancy state, knowledge lookup, session/environment context, workflow routing, command lookup, and safety posture; each entry declares canonical machine path, preferred command, preferred human path, and fallback paths
- `decision.query_output_contracts` (area: `query_runtime`; phases: `phase.3, phase.7`; upstream: `none_pack_owned`): keep one shared output baseline across offsec queries with stable id, status, summary, path, and updated_at_utc columns where meaningful; expose curated query families plus generic family-query fallback for stable derived indexes and stable raw governed records; add a public graph query surface; when status output is used for authority.offsec.safety_posture it must also expose effective_mode, interaction_mode, environment_type, safety_posture_summary, confirmation_summary, refusal_summary, and policy_refs
- `decision.challenge_index_row_contract` (area: `query_runtime`; phases: `phase.3, phase.4`; upstream: `none_pack_owned`): use rich challenge_index rows with baseline challenge_id, status, summary, canonical_path, and last_activity_at_utc plus standard platform, event, challenge_slug, challenge_title, current_workflow_id, current_session_id, blocker_count, unresolved_discrepancy_count, active_governance_limits, latest_closeout_ref, closeout_outcome, evidence_count, knowledge_candidate_count, and rendered_view_path fields where meaningful
- `decision.blocker_index_row_contract` (area: `query_runtime`; phases: `phase.3, phase.4, phase.6`; upstream: `none_pack_owned`): use rich blocker_index rows with baseline blocker_id, challenge_id, status, severity, and summary plus blocker_type, discrepancy_id, active_governance_limits, requires_review, resolution_status, opened_at_utc, last_updated_at_utc, canonical_path, rendered_view_path, source_artifact_ref, and owner_role fields where meaningful
- `decision.session_index_row_contract` (area: `query_runtime`; phases: `phase.3, phase.6`; upstream: `none_pack_owned`): use rich session_index rows with baseline session_id, challenge_id, status, requested_mode, effective_mode, environment_type, started_at_utc, last_activity_at_utc, and canonical_path plus interaction_mode, current_workflow_id, environment_summary, handoff_ready, operator_actor_ref, reviewer_actor_ref, active_governance_limits, recent_evidence_count, and last_command_summary fields where meaningful
- `decision.knowledge_index_row_contract` (area: `query_runtime`; phases: `phase.3, phase.5`; upstream: `none_pack_owned`): use rich knowledge_index rows with baseline knowledge_id, family, title, status, review_status, trust_state, verification_status, canonical_path, and updated_at_utc plus summary, tactic_refs, source_count, evidence_count, relation_count, parent_refs, freshness_bucket, and rendered_view_path fields where meaningful
- `decision.route_preview_output_contract` (area: `workflow_routing`; phases: `phase.1, phase.3`; upstream: `shared_route_preview_available`): keep route preview aligned to the live shared-core contract: JSON includes command, status, request, task_type, selected_route_count, selected_routes, selected_workflows, and warnings; selected_routes use route_id, task_type, score, matched_keywords, required_workflow_ids, and required_workflow_paths; selected_workflows use workflow_id, workflow_kind, title, doc_path, phase_type, task_family, and composes_module_paths
- `decision.workflow_metadata_contract` (area: `workflow_routing`; phases: `phase.1, phase.3`; upstream: `shared_workflow_metadata_schema_available`): use the live shared-core workflow_metadata_registry schema as the v1 pack-facing authority, with required workflow_id, phase_type, task_family, and primary_risks fields and only the shared optional fields such as extra_trigger_tags and companion_workflow_ids
- `decision.route_index_contract` (area: `workflow_routing`; phases: `phase.1, phase.3`; upstream: `shared_route_index_available`): treat the shared route_index.json as the only derived machine route surface in v1, with route_id, task_type, trigger_keywords, required_workflow_ids, and required_workflow_paths as the locked entry fields generated from authored routing tables
- `decision.shared_query_helper_contract` (area: `query_runtime`; phases: `phase.3, phase.5`; upstream: `shared_query_runtime_available`): compose offsec query helpers on top of the shared pack-agnostic query surface, keeping format as a presentation choice rather than a different retrieval operation and assuming shared lookup, filtered listing, provenance traversal, route or workflow lookup, registry lookup, and search-style filtering
- `decision.route_workflow_query_alias_policy` (area: `query_runtime`; phases: `phase.3`; upstream: `shared_query_runtime_available`): do not add pack-specific aliases for shared route or workflow query surfaces in v1; use shared route and workflow lookup directly and keep offsec-specific queries focused on offsec-owned artifacts and rendered views
- `decision.sync_target_outputs` (area: `sync_runtime`; phases: `phase.3, phase.7`; upstream: `shared_sync_primitives_available`): challenge-index rebuilds challenge indexes; knowledge-index rebuilds knowledge indexes and relation views; rendered-views rebuilds markdown companions; all runs the full set
- `decision.sync_surface_granularity` (area: `sync_runtime`; phases: `phase.3, phase.7`; upstream: `shared_sync_primitives_available`): keep the namespace minimal with challenge-index, knowledge-index, graph-index, rendered-views, and all; rely on generic shared sync surfaces for route, workflow, foundation, and reference indexes unless later operator evidence proves otherwise
- `decision.rendered_visibility_surfaces` (area: `rendered_surfaces`; phases: `phase.3, phase.4, phase.5, phase.6`; upstream: `none_pack_owned`): treat rendered visibility as first-class and require a pack-root overview plus rendered tracking summaries for challenges, knowledge, blockers, and sessions governed by rendered_surface_registry and human_surface_policy_registry
- `decision.template_contracts` (area: `document_templates`; phases: `phase.4`; upstream: `none_pack_owned`): drive challenge.md, notes.md, and recap.md through a template_catalog; require challenge.md sections Source, Objective, Constraints, and Environment; require notes.md sections Working Summary, Hypotheses, Commands, Evidence, Blockers, and Next Steps; require recap.md sections Outcome, Path, Failures, and Reusable Lessons
- `decision.notes_template_profile_model` (area: `document_templates`; phases: `phase.4`; upstream: `none_pack_owned`): use one canonical notes template with optional sections and mode-specific rendering overlays rather than multiple unrelated base templates
- `decision.markdown_reconciliation_policy` (area: `markdown_authority`; phases: `phase.2, phase.4`; upstream: `none_pack_owned`): keep challenge.md body source-faithful and locked; allow required front matter to be added at the next workflow opportunity; keep notes.md directly editable; require append-preserving agent edits; allow ad hoc user sections during active work and normalize or flag them only before closeout; hidden machine records remain authoritative for governed fields
- `decision.closeout_gate` (area: `closeout`; phases: `phase.4, phase.5`; upstream: `none_pack_owned`): require challenge.md, notes.md, closeout_record, extraction_output, passing closeout validation, and no active discrepancy carrying no_closeout; require solution and recap.md when outcome is solved, require recap.md but not solution when outcome is blocked_closeout or unresolved, and allow omission of both only for closed_without_attempt
- `decision.extraction_trigger_policy` (area: `knowledge_capture`; phases: `phase.4, phase.5`; upstream: `none_pack_owned`): allow explicit extraction at any time, run automatic extraction at closeout, and do not run periodic active-phase extraction by default; paused sessions may update metadata but do not trigger extraction unless explicitly requested
- `decision.retention_cleanup_policy` (area: `retention_and_cleanup`; phases: `phase.4, phase.6, phase.7`; upstream: `none_pack_owned`): keep solved or closed challenges at their canonical path in v1, treat archived as a logical lifecycle state rather than immediate path relocation, do not support destructive challenge deletion in v1, and require a cleanup or transfer ledger whenever retained artifacts are pruned, redacted, or moved after closeout
- `decision.artifact_payload_primitives` (area: `artifact_contracts`; phases: `phase.2, phase.4, phase.5`; upstream: `none_pack_owned`): require contract_version, RFC 3339 UTC whole-second timestamps, pack-relative POSIX paths, and structured checksum objects across governed JSON artifact families
- `decision.closeout_record_contract` (area: `closeout`; phases: `phase.4, phase.5`; upstream: `none_pack_owned`): use one shared closeout_record envelope with the Step 1 required and conditional fields, with outcome carrying solved or unresolved semantics instead of a separate solved boolean
- `decision.evidence_storage_layout` (area: `evidence_capture`; phases: `phase.4, phase.6`; upstream: `none_pack_owned`): keep raw supporting captures under offensive_security/ctf/<platform>/<event>/<challenge>/artifacts/ and store the authoritative governed evidence inventory in offensive_security/ctf/<platform>/<event>/<challenge>/.wt_local/evidence/artifacts.json as one entries[] collection keyed by artifact_id
- `decision.evidence_artifact_contract` (area: `evidence_capture`; phases: `phase.2, phase.4, phase.6`; upstream: `none_pack_owned`): use one shared evidence_artifact envelope with the Step 1 required and conditional fields, pack-relative stored paths, transfer provenance fields, and shared trust or verification vocabularies
- `decision.extraction_output_contract` (area: `knowledge_capture`; phases: `phase.4, phase.5`; upstream: `none_pack_owned`): use one shared extraction_output envelope with the Step 1 required and conditional fields, artifact-id refs instead of raw paths, and status values active, archived, and superseded
- `decision.discrepancy_record_contract` (area: `discrepancy_governance`; phases: `phase.2, phase.4, phase.6`; upstream: `none_pack_owned`): use the locked discrepancy record contract with required discrepancy_id, artifact_family, discrepancy_type, severity, detected_at, detected_by, status, source_artifact_ref, and summary fields, plus the locked conditional fields for challenge/session refs, compared values, related refs, review force, resolution, and governance limits
- `decision.discrepancy_policy` (area: `discrepancy_governance`; phases: `phase.4, phase.6`; upstream: `none_pack_owned`): use discrepancy status open, in_review, resolved, and dismissed; use severity informational, low, medium, high, and critical; use forces_needs_review for escalation; and use governance_limits for enforced restrictions instead of special severity names such as policy_block
- `decision.discrepancy_registry_contract` (area: `discrepancy_governance`; phases: `phase.2, phase.4, phase.5, phase.6`; upstream: `none_pack_owned`): publish discrepancy_type_registry, severity_registry, discrepancy_resolution_registry, and governance_limit_registry; starter discrepancy types are sync_conflict, validation_error, unknown_value, invalid_transition, status_mismatch, review_state_mismatch, provenance_gap, trust_mismatch, verification_gap, missing_required_field, invalid_relation, registry_violation, route_drift, workflow_metadata_drift, and preview_contract_drift; starter severities are informational, low, medium, high, and critical; starter resolutions are resolved, dismissed, and exceptioned
- `decision.discrepancy_exception_limits_contract` (area: `discrepancy_governance`; phases: `phase.4, phase.5, phase.6`; upstream: `none_pack_owned`): lock the starter governance_limit values to read_only_only, no_remote_execution, no_closeout, review_required, no_promotion, no_full_auto, and no_transfer, and require validators plus workflow gates to enforce active limits rather than treating them as advisory notes
- `decision.command_capture_redaction` (area: `evidence_capture`; phases: `phase.4, phase.6`; upstream: `none_pack_owned`): always capture proposed or executed command metadata and summaries; keep raw or large stdout or stderr in artifact captures instead of inline notes or event payloads; redact clearly player-owned material from routine machine-visible surfaces while allowing challenge-issued ephemeral values to remain when operationally useful; use artifact refs for bulky or sensitive outputs
- `decision.knowledge_relation_model` (area: `knowledge_graph`; phases: `phase.5`; upstream: `none_pack_owned`): publish a relation_type_registry and keep authoritative typed relations[] on the source artifact with starter relation types derived_from, supports, related_to, supersedes, belongs_to_tactic, uses_tool, uses_protocol, references, evidenced_by, produced_by_command, captured_in_event, and imported_via_transfer; keep related_artifact_ids derived-only in indexes
- `decision.promotion_policy_registry` (area: `knowledge_promotion`; phases: `phase.5`; upstream: `none_pack_owned`): add a promotion_policy_registry with one starter entry per target family; each entry declares allowed source artifact kinds, required review workflow workflow.offensivesecurity.ctf_reviewer, required provenance fields challenge_id closeout_id extraction_id source_artifact_refs and evidence_refs, mirror_mode, and auto_promotion_allowed; runtime auto-promotion stays off by default and only reference and tool_profile are eligible if a narrow allowlist is later enabled
- `decision.promotion_state_machine` (area: `knowledge_promotion`; phases: `phase.5`; upstream: `none_pack_owned`): use candidate, accepted, deprecated, and archived as the reusable-knowledge lifecycle baseline, with review_required and review_status carrying review progress separately and promotion expressed as the governed candidate-to-accepted transition
- `decision.retrieval_ranking_contract` (area: `knowledge_retrieval`; phases: `phase.5`; upstream: `none_pack_owned`): default retrieval uses accepted artifacts only and ranks by scope match, exact tool/protocol match over general tactics, reusability, freshness, evidence quality, breadth of applicability, teacher-mode explanation boost when active, then deterministic knowledge_id tie-break
- `decision.review_status_registry` (area: `review_governance`; phases: `phase.2, phase.4, phase.5`; upstream: `none_pack_owned`): keep review_required separate from lifecycle status and publish a review_status registry with not_required, pending_review, in_review, approved, rejected, and approved_with_exception, allowing per-family subsets for knowledge, closeout, discrepancy, and workflow-review surfaces
- `decision.source_clarification_policy` (area: `provenance`; phases: `phase.4, phase.5, phase.6`; upstream: `none_pack_owned`): allow targeted minimal clarification only when source, classification, environment, trust, verification, path, or id metadata materially affects indexing, review, promotion, closeout, safety, or canonical record creation; in assistant, teacher, and note_taker modes ask before governed record creation when the missing metadata is material; in full_auto use provisional values and trigger needs_review or discrepancy handling instead of fabricating certainty
- `decision.external_references_schema` (area: `knowledge_provenance`; phases: `phase.2, phase.5`; upstream: `none_pack_owned`): use one shared external_references entry shape across knowledge families with required url and reference_kind, strongly recommended label, and optional is_primary and quality_notes; keep reference_kind separate from source.type and start with official, official_standard, vendor, high_quality_reference, community, and archive
- `decision.candidate_knowledge_representation` (area: `knowledge_promotion`; phases: `phase.4, phase.5`; upstream: `none_pack_owned`): create extracted knowledge artifacts directly in their target family at the canonical final path, start with status candidate, and promote in place to status accepted rather than creating a separate candidate family or duplicate accepted artifact
- `decision.shared_knowledge_envelope` (area: `knowledge_contracts`; phases: `phase.2, phase.5`; upstream: `none_pack_owned`): use one shared reusable-knowledge envelope across command, tool_profile, protocol, reference, tactic, and playbook families before adding family-specific payload fields
- `decision.command_payload_contract` (area: `knowledge_contracts`; phases: `phase.5`; upstream: `none_pack_owned`): lock the command family to command_template, placeholders, required_binary, supported_environment_types, shell_assumptions, safety_class, when_to_use, and when_not_to_use, with compact optional execution details layered on top
- `decision.tool_profile_payload_contract` (area: `knowledge_contracts`; phases: `phase.5`; upstream: `none_pack_owned`): lock the tool_profile family to tool_binary, supported_environment_types, when_to_use, when_not_to_use, applicability_boundaries, strengths, limitations, and prerequisites, with optional operational notes layered on top
- `decision.protocol_payload_contract` (area: `knowledge_contracts`; phases: `phase.5`; upstream: `none_pack_owned`): lock the protocol family to default_ports, transport, service_identity, when_to_use, when_not_to_use, operator_relevance, authentication_notes, enumeration_notes, and attack_relevance, with optional misconfiguration and verification fields layered on top
- `decision.reference_payload_contract` (area: `knowledge_contracts`; phases: `phase.5`; upstream: `none_pack_owned`): lock the reference family to source_kind, source_author_or_publisher, publication_context, key_takeaways, operator_relevance, when_to_use, when_not_to_use, and not_normative_for, with optional quality and trust notes layered on top
- `decision.tactic_payload_contract` (area: `knowledge_contracts`; phases: `phase.5`; upstream: `none_pack_owned`): lock the tactic family to the Step 1 orienting payload fields and canonical path offensive_security/knowledge/tactics/<tactic_slug>/overview.md, keeping tactics coarse and navigation-oriented
- `decision.playbook_payload_contract` (area: `knowledge_contracts`; phases: `phase.5`; upstream: `none_pack_owned`): lock the playbook family to the Step 1 procedure-first payload fields and canonical path offensive_security/knowledge/tactics/<tactic_slug>/playbooks/<playbook_slug>.md, keeping playbooks structured and reproducible
- `decision.glossary_term_registry_contract` (area: `vocabulary_governance`; phases: `phase.2, phase.5`; upstream: `none_pack_owned`): ship one canonical human-readable glossary standard, one pack-facing glossary surface, and one machine-readable term registry with deprecation and replacement fields; default implementation paths are offensive_security/docs/standards/metadata/offsec_glossary_standard.md, offensive_security/docs/references/offsec_glossary.md, and offensive_security/.wt/registries/term_registry.json
- `decision.environment_adapter_protocol` (area: `environment_adapters`; phases: `phase.6`; upstream: `none_pack_owned`): local, vpn_reachable, ssh, and airgapped adapters expose describe_context, check_capability, execute_command, capture_artifact, stage_transfer, and record_provenance; local and remote adapters stay guidance-only by default until stronger autonomy is explicitly enabled, and airgapped omits live execution
- `decision.safety_confirmation_matrix` (area: `safety_policy`; phases: `phase.6`; upstream: `none_pack_owned`): classify actions as read_only, state_changing, sensitive, or destructive; require confirmation for every mode or autonomy change, credential use, privilege escalation, remote state-changing actions, long-running automation, external transfer, and destructive cleanup; refuse destructive unattended full_auto and other actions blocked by scope ambiguity, insufficient environment context, provenance gaps, repeated failures, or stale state
- `decision.safety_confirmation_matrix_contract` (area: `safety_policy`; phases: `phase.6`; upstream: `none_pack_owned`): use one pack-local safety_confirmation_matrix policy artifact with entries keyed by rule_id and explicit selector/enforcement fields such as action_classification, confirmation_required, applies_to, interaction_modes, environment_types, remote execution, privilege escalation, credential, transfer, and refusal metadata
- `decision.mode_state_projection` (area: `mode_and_session_state`; phases: `phase.4, phase.6`; upstream: `none_pack_owned`): record both requested_mode and effective_mode in session_state and the event_stream; mirror effective mode, autonomy level, and recent transition summary into visible session surfaces only on meaningful changes
- `decision.full_auto_observability_contract` (area: `mode_and_observability`; phases: `phase.3, phase.6`; upstream: `none_pack_owned`): full-auto must show planned commands before execution, concise results after execution, grouped checkpoints, and a live journal, while pausing at risk or scope boundaries and allowing the operator to choose the detail level
- `decision.airgapped_transfer_contract` (area: `airgapped_workflow`; phases: `phase.6`; upstream: `none_pack_owned`): require transfer_id, direction, operator, source_system, destination_system, timestamp_utc, purpose, and artifact_refs for every manual transfer at offensive_security/.wt/ledgers/transfers/<transfer_id>.json; capture sha256 when practical; quarantine imported or pasted claims-bearing material at lower trust until manually verified or reproduced; require re-paste before machine-state updates when structured pasted output is malformed or partial; and index claims-bearing transferred artifacts
- `decision.actor_ref_requirement` (area: `actor_validation`; phases: `phase.2, phase.6`; upstream: `shared_actor_registry_available_pack_policy_pending`): require actor refs on mode changes, confirmations and overrides, promotion approvals, closeout approvals or finalization, delegated or remote execution, airgapped imports/exports, and destructive or retention actions; allow omission only on low-risk note-only updates and purely derived refresh work; unresolved required refs are validation errors after bootstrap
- `decision.workflow_catalog` (area: `workflow_governance`; phases: `phase.3`; upstream: `none_required_for_v1`): defer richer workflow_catalog beyond routing-table plus workflow-metadata baseline; when revisited, add titles, summaries, inputs, outputs, owner, version, workflow-surface links, composition/dependency metadata, and a generated human explorer over a machine-primary catalog
- `decision.actor_bootstrap_day_one` (area: `actor_validation`; phases: `phase.2, phase.6`; upstream: `pack implementation need`): reuse shared actor_registry surface and defer actor bootstrap unless strict actor-ref validation is required immediately
- `decision.public_rebuild_cli` (area: `sync_rebuild_contract`; phases: `phase.3, phase.7`; upstream: `shared_cli_evolution`): keep public operator guidance aligned to sync
- `decision.saved_query_views` (area: `post_v1_operator_ux`; phases: `phase.3, phase.7`; upstream: `real_usage_signal`): defer saved query views beyond v1; when added, ship pack-owned defaults first, user-local customization later, and keep user-local saved views outside the governed pack root in user-local config
- `decision.provenance_review_impact_surface` (area: `post_v1_governance`; phases: `phase.5, phase.6, phase.7`; upstream: `real_provenance_usage`): defer richer provenance/trust workflows beyond v1; when added, prefer provenance-triggered re-review, downstream impact analysis, and a reviewer-facing affected-downstream view while flagging accepted knowledge for review rather than auto-invalidating it
- `decision.pentest_pack_split` (area: `future_pack_topology`; phases: `phase.0, phase.5`; upstream: `future_product_scope`): start with one offensive_security hosted pack and revisit later

## Locked Post-V1 Deferrals

The preserved intentional deferral set remains:

This section is a scan-friendly summary of deferrals already present in the exact normalized decision register above. It intentionally repeats a subset of preserved decision ids and should not be used as a second counting surface when checking appendix parity against `open_decisions.json.raw`.

- `decision.workflow_catalog`: defer a richer workflow catalog beyond routing-table plus workflow-metadata baseline.
- `decision.actor_bootstrap_day_one`: reuse shared `actor_registry` and defer actor bootstrap unless needed immediately.
- `decision.public_rebuild_cli`: keep public operator guidance centered on `sync`.
- `decision.pentest_pack_split`: keep one `offensive_security` hosted pack for v1.
- `decision.saved_query_views`: defer saved query views beyond v1, with pack-owned defaults first and user-local views outside the governed pack root later.
- `decision.provenance_review_impact_surface`: defer richer provenance-triggered downstream review tooling beyond v1.

## Conditional Revisit Queue

The initiative now carries `conditional_revisit_queue.md` as the operator-facing companion for these non-blocking later decisions.

- It does not reopen any locked default by itself.
- It records the trigger that must fire before a later implementation change is allowed to override the preserved baseline.
- If a trigger fires, record a new live-contract delta first, then update the affected canonical docs, live task state, and machine-readable acceptance or evidence surfaces in the same change set.

## Propagation Rule

The imported package already normalized its workshop decisions into contracts, phases, workflows, standards, and indexes. This initiative preserves that rule:

- no standalone workshop log is resurrected as an authority surface;
- the canonical record of each locked default remains the directly affected contract, phase, workflow, standard, or canonical initiative doc; and
- if future implementation overrides any locked default, the change must be recorded as a new live-contract delta plus an update to the preserved decision register and its canonical initiative reflections.
